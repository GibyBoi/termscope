# --- Node / Vite (frontend) ---
node_modules/
dist/
.vite/

# --- Rust / Tauri (backend) ---
src-tauri/target/
src-tauri/gen/schemas/

# --- Python (legacy app under legacy/) ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Personal / progress / state — NEVER upload.
# (These normally live in %APPDATA%\TermScope\, outside the repo; listed here so
#  they're still excluded if a copy ever lands in the project folder.)
knowledge.json
config.json
termscope.log
*.log
models/

# Local artifacts from the manual audio tests under legacy/tests/manual/
*.wav
stress_test_log.jsonl
